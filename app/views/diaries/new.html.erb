<%= l(@created_on) %>
日記新規作成ページ
<%= form_with model: [@diary], local: true do |f| %>
今日のメモ
<%= f.text_area :text, placeholder:"今日あった出来事を残しておきましょう"%>

体重
<%= f.text_field :weight %>g

  <%# すでに今日のご飯記録作成済みの場合 %>
  <% if (FeedManagement.exists?) && @created_on == @last_data.created_on %>
  食べた餌の量<%= @last_data.amount_eaten %>g
    →<%= link_to "ご飯記録を更新する", edit_feed_management_path(@last_data.id, value:"日記作成ページ経由") %>

  <%# 今日のご飯記録未作成の場合 %>
  <% else %>
  食べた餌の量
    今日のご飯記録が未作成です
    →<%= link_to "ご飯記録を作成する", new_feed_management_path(value:"日記作成ページ経由") %>
  <% end %>

体調チェック
→<%= link_to "体調チェックをする", '#'%>

<% end %>


